# Database - Shared MongoDB Atlas (you already configured this to allow all IPs)
MONGODB_URI=mongodb+srv://dawsonlee0512_db_user:PASSWORD@cluster0.u7cvnl3.mongodb.net/ai-study-assistant

# Google Gemini API - Each teammate needs their own API key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# AWS S3 Configuration - Shared credentials (or each person can use their own)
# Bucket: ai-study-buddy-5500
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=ai-study-buddy-5500

# JWT Secret - Can be shared for development (use different in production)
JWT_SECRET=change-this-to-a-random-secret-key-in-production

# Admin Configuration - Set to email of admin user
ADMIN_EMAIL=admin@example.com
